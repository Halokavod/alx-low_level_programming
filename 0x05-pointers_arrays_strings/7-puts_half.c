#include "main.h"


/**
 * puts_half - update value.
 * @str: value to be evaluate.
 * Return: not.
 */

void puts_half(char *str)
{
	 int count;

	 for (count = 0; *str[count] != '\0'; count++);



	return (0);
}

