#include "main.h"
#include <stdio.h>

/**
 *  _puts - print a string followed by a new line
 *
 *  @str: an inputted string
 *
 *  Return: nothing
 */

void _puts(char *str)
{
	char *str

	str = 0;

	while (*(str + 1) != '\0')
	{
		_puts(str[i]);
		str++;
	}
	_puts('\n');
}
